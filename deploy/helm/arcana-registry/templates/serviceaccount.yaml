{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "arcana-registry.serviceAccountName" . }}
  labels:
    {{- include "arcana-registry.labels" . | nindent 4 }}
{{- end }}
