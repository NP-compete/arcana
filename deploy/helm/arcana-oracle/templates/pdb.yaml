{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "arcana-oracle.fullname" . }}
  labels:
    {{- include "arcana-oracle.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "arcana-oracle.selectorLabels" . | nindent 6 }}
{{- end }}
