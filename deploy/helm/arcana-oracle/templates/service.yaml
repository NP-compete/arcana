apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-oracle.fullname" . }}
  labels:
    {{- include "arcana-oracle.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-oracle.selectorLabels" . | nindent 4 }}
