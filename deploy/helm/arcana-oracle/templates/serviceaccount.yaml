{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{{ include "arcana-oracle.serviceAccountName" . }}}
  labels:
    {{- include "arcana-oracle.labels" . | nindent 4 }}
{{- end }}
