{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{{ include "arcana-memory.serviceAccountName" . }}}
  labels:
    {{- include "arcana-memory.labels" . | nindent 4 }}
{{- end }}
