{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{{ include "arcana-tools.serviceAccountName" . }}}
  labels:
    {{- include "arcana-tools.labels" . | nindent 4 }}
{{- end }}
