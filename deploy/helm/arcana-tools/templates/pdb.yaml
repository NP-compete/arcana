{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "arcana-tools.fullname" . }}
  labels:
    {{- include "arcana-tools.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "arcana-tools.selectorLabels" . | nindent 6 }}
{{- end }}
