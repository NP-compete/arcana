apiVersion: v1
kind: Service
metadata:
  name: {{{ include "arcana-codex-router.fullname" . }}}
  labels:
    {{- include "arcana-codex-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-codex-router.selectorLabels" . | nindent 4 }}
