{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "arcana-probe.fullname" . }}
  labels:
    {{- include "arcana-probe.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "arcana-probe.selectorLabels" . | nindent 6 }}
{{- end }}
