apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{{ include "arcana-probe.fullname" . }}}
  labels:
    {{- include "arcana-probe.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "arcana-probe.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "arcana-probe.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{{ include "arcana-probe.serviceAccountName" . }}}
      containers:
        - name: probe
          image: {{{ include "arcana-probe.image" . }}}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness | default "/healthz" }}
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness | default "/readyz" }}
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
