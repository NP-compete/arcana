apiVersion: v1
kind: Service
metadata:
  name: {{{ include "arcana-probe.fullname" . }}}
  labels:
    {{- include "arcana-probe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-probe.selectorLabels" . | nindent 4 }}
