apiVersion: v1
kind: Service
metadata:
  name: {{{ include "arcana-codex-searcher.fullname" . }}}
  labels:
    {{- include "arcana-codex-searcher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-codex-searcher.selectorLabels" . | nindent 4 }}
