{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "arcana-finops.serviceAccountName" . }}
  labels:
    {{- include "arcana-finops.labels" . | nindent 4 }}
{{- end }}
