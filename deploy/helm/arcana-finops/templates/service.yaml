apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-finops.fullname" . }}
  labels:
    {{- include "arcana-finops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-finops.selectorLabels" . | nindent 4 }}
