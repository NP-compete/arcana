apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-api.fullname" . }}
  labels:
    {{- include "arcana-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-api.selectorLabels" . | nindent 4 }}
