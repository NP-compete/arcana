{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "arcana-operator.serviceAccountName" . }}
  labels:
    {{- include "arcana-operator.labels" . | nindent 4 }}
{{- end }}
