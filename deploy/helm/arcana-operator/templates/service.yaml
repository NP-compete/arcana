apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-operator.fullname" . }}
  labels:
    {{- include "arcana-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-operator.selectorLabels" . | nindent 4 }}
