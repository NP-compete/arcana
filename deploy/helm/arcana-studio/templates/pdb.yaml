{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "arcana-studio.fullname" . }}
  labels:
    {{- include "arcana-studio.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "arcana-studio.selectorLabels" . | nindent 6 }}
{{- end }}
