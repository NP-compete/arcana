{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "arcana-studio.serviceAccountName" . }}
  labels:
    {{- include "arcana-studio.labels" . | nindent 4 }}
{{- end }}
