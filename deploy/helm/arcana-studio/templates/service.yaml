apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-studio.fullname" . }}
  labels:
    {{- include "arcana-studio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-studio.selectorLabels" . | nindent 4 }}
