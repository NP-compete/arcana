apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-mesh.fullname" . }}
  labels:
    {{- include "arcana-mesh.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-mesh.selectorLabels" . | nindent 4 }}
