{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{{ include "arcana-forge.serviceAccountName" . }}}
  labels:
    {{- include "arcana-forge.labels" . | nindent 4 }}
{{- end }}
