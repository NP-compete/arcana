apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-forge.fullname" . }}
  labels:
    {{- include "arcana-forge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-forge.selectorLabels" . | nindent 4 }}
