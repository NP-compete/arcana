{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "arcana-annotate.fullname" . }}
  labels:
    {{- include "arcana-annotate.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "arcana-annotate.selectorLabels" . | nindent 6 }}
{{- end }}
