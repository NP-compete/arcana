{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{{ include "arcana-scheduler.serviceAccountName" . }}}
  labels:
    {{- include "arcana-scheduler.labels" . | nindent 4 }}
{{- end }}
