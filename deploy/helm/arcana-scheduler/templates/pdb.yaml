{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "arcana-scheduler.fullname" . }}
  labels:
    {{- include "arcana-scheduler.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "arcana-scheduler.selectorLabels" . | nindent 6 }}
{{- end }}
