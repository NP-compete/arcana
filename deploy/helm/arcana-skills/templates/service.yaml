apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-skills.fullname" . }}
  labels:
    {{- include "arcana-skills.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-skills.selectorLabels" . | nindent 4 }}
