apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "arcana-skills.fullname" . }}
  labels:
    {{- include "arcana-skills.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "arcana-skills.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "arcana-skills.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "arcana-skills.serviceAccountName" . }}
      containers:
        - name: skills
          image: {{ include "arcana-skills.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness | default "/healthz" }}
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness | default "/readyz" }}
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
