{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "arcana-connectors.fullname" . }}
  labels:
    {{- include "arcana-connectors.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "arcana-connectors.selectorLabels" . | nindent 6 }}
{{- end }}
