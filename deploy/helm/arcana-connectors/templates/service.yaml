apiVersion: v1
kind: Service
metadata:
  name: {{{ include "arcana-connectors.fullname" . }}}
  labels:
    {{- include "arcana-connectors.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-connectors.selectorLabels" . | nindent 4 }}
