{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{{ include "arcana-connectors.serviceAccountName" . }}}
  labels:
    {{- include "arcana-connectors.labels" . | nindent 4 }}
{{- end }}
