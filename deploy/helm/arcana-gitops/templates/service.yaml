apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-gitops.fullname" . }}
  labels:
    {{- include "arcana-gitops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-gitops.selectorLabels" . | nindent 4 }}
