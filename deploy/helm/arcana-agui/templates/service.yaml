apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-agui.fullname" . }}
  labels:
    {{- include "arcana-agui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-agui.selectorLabels" . | nindent 4 }}
