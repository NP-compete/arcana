apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-graph.fullname" . }}
  labels:
    {{- include "arcana-graph.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-graph.selectorLabels" . | nindent 4 }}
