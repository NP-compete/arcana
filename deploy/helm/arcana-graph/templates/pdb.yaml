{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "arcana-graph.fullname" . }}
  labels:
    {{- include "arcana-graph.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "arcana-graph.selectorLabels" . | nindent 6 }}
{{- end }}
