{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{{ include "arcana-graph.serviceAccountName" . }}}
  labels:
    {{- include "arcana-graph.labels" . | nindent 4 }}
{{- end }}
