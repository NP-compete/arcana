apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-models.fullname" . }}
  labels:
    {{- include "arcana-models.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-models.selectorLabels" . | nindent 4 }}
