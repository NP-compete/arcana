{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "arcana-blueprint.serviceAccountName" . }}
  labels:
    {{- include "arcana-blueprint.labels" . | nindent 4 }}
{{- end }}
