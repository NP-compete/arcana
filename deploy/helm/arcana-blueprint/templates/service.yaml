apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-blueprint.fullname" . }}
  labels:
    {{- include "arcana-blueprint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-blueprint.selectorLabels" . | nindent 4 }}
