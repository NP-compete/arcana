{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "arcana-audit.fullname" . }}
  labels:
    {{- include "arcana-audit.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "arcana-audit.selectorLabels" . | nindent 6 }}
{{- end }}
