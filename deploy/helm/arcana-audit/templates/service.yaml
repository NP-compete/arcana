apiVersion: v1
kind: Service
metadata:
  name: {{ include "arcana-audit.fullname" . }}
  labels:
    {{- include "arcana-audit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arcana-audit.selectorLabels" . | nindent 4 }}
