apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "arcana-ward.fullname" . }}
  labels:
    {{- include "arcana-ward.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "arcana-ward.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "arcana-ward.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "arcana-ward.serviceAccountName" . }}
      containers:
        - name: ward
          image: {{ include "arcana-ward.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness | default "/healthz" }}
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness | default "/readyz" }}
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
