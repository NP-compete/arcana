{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "arcana-ward.serviceAccountName" . }}
  labels:
    {{- include "arcana-ward.labels" . | nindent 4 }}
{{- end }}
