{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "arcana-engine.fullname" . }}
  labels:
    {{- include "arcana-engine.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "arcana-engine.selectorLabels" . | nindent 6 }}
{{- end }}
