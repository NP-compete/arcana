{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "arcana-engine.serviceAccountName" . }}
  labels:
    {{- include "arcana-engine.labels" . | nindent 4 }}
{{- end }}
