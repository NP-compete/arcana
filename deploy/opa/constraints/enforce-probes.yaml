apiVersion: constraints.gatekeeper.sh/v1beta1
kind: ArcanaRequireProbes
metadata:
  name: enforce-probes
spec:
  match:
    kinds:
      - apiGroups: ["apps"]
        kinds: ["Deployment"]
    namespaces: ["arcana"]
