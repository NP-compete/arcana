apiVersion: constraints.gatekeeper.sh/v1beta1
kind: ArcanaRequireWard
metadata:
  name: require-ward-on-agents
spec:
  match:
    kinds:
      - apiGroups: ["arcana.io"]
        kinds: ["ArcanaAgent"]
